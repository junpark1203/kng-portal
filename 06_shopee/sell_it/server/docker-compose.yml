services:
  sell-it-server:
    image: ghcr.io/junpark1203/sell-it-server:latest
    container_name: sell_it_server
    restart: always
    volumes:
      - ./data:/usr/src/app/data
      - ./uploads:/usr/src/app/uploads
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
    labels:
      - "com.centurylinklabs.watchtower.scope=sell-it"

  cloudflared:
    image: mirror.gcr.io/cloudflare/cloudflared:latest
    container_name: sell_it_cloudflared
    restart: always
    command: tunnel run
    environment:
      - TUNNEL_TOKEN=${TUNNEL_TOKEN}
    depends_on:
      sell-it-server:
        condition: service_healthy

  watchtower:
    image: ghcr.io/containrrr/watchtower:1.7.1
    container_name: sell_it_watchtower
    restart: always
    environment:
      - WATCHTOWER_POLL_INTERVAL=86400
      - WATCHTOWER_CLEANUP=true
      - WATCHTOWER_SCOPE=sell-it
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    labels:
      - "com.centurylinklabs.watchtower.scope=sell-it"

